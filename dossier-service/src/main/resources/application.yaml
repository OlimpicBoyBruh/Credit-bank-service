server:
  port: 8095

spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: "dealGroup"
    bootstrap-servers: localhost:29092

  mail:
    host: smtp.gmail.com
    port: 587
    test-connection: true
    username: javaspringtest17@gmail.com
    password: "osfg qxra otwj pntb"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

integration:
  deal:
    base-url: http://localhost:8085
    method:
      path-statement: /deal/statement/get
      path-update-status: /deal/admin/statement/{statementId}/status
